---
title: "SDK Quickstart"
sidebarTitle: "SDK Quickstart"  
description: "Build your first workflow in 5 minutes using Python"
icon: rocket
---

Build and run your first Kubiya workflow in Python.

## Prerequisites
- Python 3.8+ installed
- Get your API key from [compose.kubiya.ai](https://compose.kubiya.ai)

## 1. Install and setup

```bash
pip install kubiya-sdk
export KUBIYA_API_KEY="your-api-key"
```

## 2. Create your first workflow

```python
from kubiya_sdk import workflow, run

@workflow
def hello_world():
    return "Hello from Kubiya! 🚀"

# Run it
result = run(hello_world)
print(result)
```

## 3. Try something useful

```python
@workflow  
def check_system():
    return "df -h"  # Check disk space

result = run(check_system)
print(result)
```

## Next steps

- **[Try Composer →](/composer/getting-started)** - Visual interface (easier to start with)
- **[See more examples →](/workflows/examples)** - Real workflow patterns
- **[Read the full docs →](/concepts/workflows)** - Complete SDK reference

---

**Prefer visual tools?** Most users find [Composer](/composer/overview) easier to start with since you can see workflows as you build them.